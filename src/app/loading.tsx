export default function Loading() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <span className="text-sm tracking-[0.25em] uppercase text-ink-dim animate-pulse">
        Kelvren
      </span>
    </main>
  );
}
